@model IEnumerable<TechnicalSchool.Models.Inscricao>

@{
    ViewBag.Title = "Inscrições";
}

<h2>Inscricoes</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Curso.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estudante.Sobrenome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Curso.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estudante.Sobrenome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InscricaoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.InscricaoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InscricaoID })
        </td>
    </tr>
}

</table>
